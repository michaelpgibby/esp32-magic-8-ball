# Copy this file to secrets.yaml and replace every example value.
# Never commit your real secrets.yaml file.
wifi_ssid: "YOUR_WIFI_NAME"
wifi_password: "YOUR_WIFI_PASSWORD"
fallback_ap_password: "CHANGE_ME_FALLBACK_PASSWORD"

# Generate with: openssl rand -base64 32
api_encryption_key: "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="

# Generate with: openssl rand -hex 32
ota_password: "CHANGE_ME_OTA_PASSWORD"

web_username: "admin"
web_password: "CHANGE_ME_WEB_PASSWORD"
