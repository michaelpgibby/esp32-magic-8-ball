# Assembly images

Upload assembly photos to this folder as JPEG or PNG files. Use short,
lowercase names such as `01-printed-parts.jpg`, `02-wiring.jpg`, and
`03-final-assembly.jpg` so the build sequence stays clear.

Before uploading, remove location metadata and check that photos do not reveal
credentials, addresses, faces, or other private information.
